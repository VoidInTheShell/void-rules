# 规则数据许可证说明

根目录 `LICENSE` 只授权本仓库自行编写的聚合器、schema、测试和工作流，不会改变任何上游规则数据的许可证。

`dist/` 是多个独立来源与本仓库 overlay 的机械聚合结果。每次构建都会在对应的 `manifest.json` 和 `generated/sources.lock.json` 中记录来源 URL、版本、摘要和已声明的 SPDX 标识；使用者应同时遵守所有适用的上游条款。来源未声明许可证时标记为 `NOASSERTION`，本仓库不代替来源作者作许可证推断。

`overlays/` 中由 VoidInTheShell 自行维护的规则按根目录 MIT License 提供，除非具体文件另有说明。

上游规则通常是域名、地址或匹配表达式等事实性数据；不同司法辖区对其著作权保护可能不同。本说明不构成法律意见，也不把第三方材料重新许可为 MIT。
